import React from 'react';
import Layout from '../../components/Layout';
import './style.css';

const Login = ({props}) => {
    return (
        <Layout>
            <div>
            Login Page
        </div>
        </Layout>
        
    )
}

export default Login
